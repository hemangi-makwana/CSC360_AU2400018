##   Class reflection

## Main Topics

* Basics of Computer Graphics and Drawing Shapes
* Tree Structures and Hierarchy
* Maven and Java Project Management
* GUI Development with Java Swing
* Accessibility in Software
* Processes and Multithreading

## What I Learned

### Computer Graphics

One of the areas studied was Computer Graphics in which we explored how simple shapes can be produced by programming. We started with simple shapes such as squares and triangles. I knew that to draw something on a screen would not be enough; we would have to use coordinates and tell the computer where the different parts would need to go.

The discussion on trees proved interesting too, as a tree in computer science doesn't necessarily have to be a tree. It is a representation of information in a hierarchical manner. The concept of having one or more children as a parent helped me to understand that among elements, relationships can be represented.


### GUI and Accessibility

GUI development using Java Swing was another topic studied. We studied components like buttons, panels and lists, and I realized that these components were used to make an interface on which users can interact with a program.

One of the things we talked about was accessibility, and I thought about GUI design from the user's perspective. A program should be good in appearance, have the correct functionality, and be usable by users with differing needs. Assistive technologies can help users who employ them read and comprehend digital content, and features like alternative text improve their readability.

### Maven and Java Projects

I also learned about Maven that I was not very familiar with. I learned that Maven is a tool used to manage a Java project and automate a few tasks that are normally done when building a Java project. The pom.xml file is crucial as it carries information about the project and the library/dependencies required by the project.

An additional thing I learned is that there is a difference between the files we write which are the sources and the files created during the build. For instance, Java code is compiled into .class files and written in .java files. A lot of the maven files generated are kept inside the target folder. Knowing this helped me to be less confused by the structure of a java project.

### Processes and Threads

Another significant aspect of the class was the introduction of concepts related to processes and threads. I knew that process is a program running at this moment and threads are a bunch of tasks for the program to carry out.

We used a single threaded program and a multithreaded program. A program with multiple threads can execute different programs without doing it in a linear sequence. Using multiple threads introduces potential issues, however, if they attempt to access the same data simultaneously. This is where thread safety comes in handy.

## My Takeaway

Overall, I think that I got something useful out of today's class, since it wasn't dedicated to one thing, but spread across several aspects of java programming. I got a good lesson in the graphics part about how coordinates and basic shapes can be used to make the visual elements. I also got a good insight into what goes on behind the scenes of building a Java project when I learned more about Maven.

The GUI section got me to understand that programming is also about making something that they can interact with and in the discussion about accessibility, I understood that software should be designed for broader audiences. Lastly, after learning about threads, I was able to grasp how multiple tasks can be handled in programs and the need for extra care regarding sharing of resources.

I gained an understanding of various aspects of Java development, including coding and compilation, interface design, and program execution.


